#!/usr/bin/env sh

join_args() {
    joiner="$1"
    shift

    printf "%s" "$1"
    shift

    while [ $# -gt 0 ]; do
        printf "%s%s" "$joiner" "$1"
        shift
    done
}

wget "https://www.gitignore.io/api/$(join_args "," "$@")" -O .gitignore
