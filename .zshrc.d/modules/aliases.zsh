# tmux
alias tmux="tmux -2 -u"
alias tn="tmux new-session -s"
alias tl="tmux list-sessions"
alias ta="tmux attach-session -t"

# nvim
alias n="nvim"

# git
alias gi="git init"
alias ga="git add"
alias gc="git commit"
alias gcam="gc -am"
alias gam="gc --amend --no-edit"
alias gd="git diff"
alias gst="git status"
alias gp="git push"

# python
alias pi="python3 -m pip install --user"
